function Footer() {
  // w-full = width of entire view port
  // h-[50px] = height of 50 px
  // it is fixed to the bottom of the document by having everything have h-screen
  // bg-[#999999] = background of gray
  return (
    <footer className="w-full h-[50px] bg-[#999999]">
    </footer>
  );
}

export default Footer;